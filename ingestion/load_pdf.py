from pathlib import Path


def load_pdf(path: str | Path) -> str:
    """Placeholder PDF loader for future technical manual ingestion."""
    return f"PDF loading is not implemented yet for {Path(path).name}."
